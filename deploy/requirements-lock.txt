# ─────────────────────────────────────────────────────────────
# requirements-lock.txt — 可重現性鎖檔（pin 死全部版本）
#
# 產生環境：macOS / Python 3.11 / 本機 .venv，2026-06-15 重建。
# 真相來源為 pyproject.toml；此檔僅為「某次已驗證環境」的完整快照，供需要逐版重現時用。
#
# 範圍：對應 `pip install -e .` 核心 + 目前已裝的 dev 視覺化(matplotlib/mplfinance)+pytest。
#   ※ 不含 research（vectorbt/quantstats/optuna）與 broker（shioaji）——目前環境未安裝。
#     要含回測棧請先 `pip install -e ".[dev]"` 再重產此鎖檔。
#
# 重產：.venv/bin/python -m pip freeze --exclude-editable | sort > deploy/requirements-lock.txt
#       （再補回本 header）
# 逐版重現：pip install -r deploy/requirements-lock.txt
# ─────────────────────────────────────────────────────────────
APScheduler==3.11.2
certifi==2026.5.20
charset-normalizer==3.4.7
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
fugle-marketdata==2.4.1
holidays==0.98
idna==3.18
iniconfig==2.3.0
kiwisolver==1.5.0
loguru==0.7.3
lxml==6.1.1
matplotlib==3.11.0
mplfinance==0.12.10b0
numpy==1.26.4
orjson==3.11.9
packaging==26.2
pandas==2.1.4
pillow==12.2.0
pluggy==1.6.0
pyee==11.1.1
Pygments==2.20.0
pyparsing==3.3.2
pytest==9.1.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
pytz==2026.2
requests==2.34.2
six==1.17.0
typing_extensions==4.15.0
tzdata==2026.2
tzlocal==5.3.1
urllib3==2.7.0
websocket-client==1.9.0
