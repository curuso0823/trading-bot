# ─────────────────────────────────────────────────────────────
# Taiwan trading bot — 核心依賴（已在 Python 3.11 實測可裝）
#
# ⚠️ 必須用 Python 3.11（非機器預設的 3.14）。建立 venv：
#     py -3.11 -m venv .venv
#     .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# ⚠️ numpy 必須 <2、pandas 必須 <3：vectorbt/還原計算/舊生態相依。
# 分階段的脆弱依賴（回測/下單/通知）見 requirements-phased.txt。
# ─────────────────────────────────────────────────────────────

# 數值 / 資料
numpy<2                 # 1.26.4 實測；勿升 numpy 2（破壞 vectorbt 等）
pandas==2.1.4           # 勿升 pandas 3.x
requests>=2.31.0
lxml>=5.0               # pd.read_html 解析上市/上櫃股票清單（universe.py）需要

# 資料源
fugle-marketdata>=1.0.0
# 註：FinMind 以 raw requests 直連，不需要 finmind 套件
# 註：技術指標已改用純 pandas（tech_signal.py），移除 pandas-ta
#     （PyPI 對 Python 3.11 已無可用版本）

# 排程
APScheduler>=3.10.0
pytz

# 可視化（開發期肉眼驗證）
mplfinance>=0.12.10b
matplotlib>=3.7.0

# 工具
python-dotenv>=1.0.0
pyyaml>=6.0
loguru>=0.7.0
holidays>=0.50            # 台股國定假日判斷（is_trading_day）
