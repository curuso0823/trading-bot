# CLAUDE.md — 新 session 開工前必讀（專案真相與紀律）

> 本檔由 Claude Code 在每個 session 啟動時自動載入。**這是本專案的最高優先脈絡**：以下「現況真相」凌駕任何舊文件/舊回測數字。

---

## 🟥 現況真相（2026-06-23，**M5 6 資產 allocator 已上線 paper** + R0–R6 重建收束 + E1+E2 落地 + E4–E8 事件偵測全 FAILED 歸檔後）— 不可被舊污染結論帶偏

- **🟢 現行 LIVE＝6 資產 Asset Allocator（2026-06-23 使用者拍板部署、paper 評估中）— 這是最新狀態，凌駕下方 R6 benchmark 敘述（R6 被動為前一階段、已被取代）**：
  `config/settings.yaml strategy.mode: allocator`。書＝**0050 35% / 00981A 16% / 00991A 16% / 00635U(黃金) 10% / 00864B(美債) 11.5% / MMF(合成台幣現金 sleeve) 11.5%**；本金 **NT$150,000**、mode=paper（Fugle 盤中零股成交價、模擬撮合）。初始建倉腳本 `deploy/build_initial_book.py --execute`（停 bot 下、含 00864B 冷啟腿）。
  - **三層全開**：M0（不對稱帶寬再平衡）+ M1（0050 連 3 日跌破 MA200×0.99 確認 → 股票腿 ×0.75 de-risk，源 **FinMind**）+ M2（CPI+Fed 雙確認 USD regime → ±5pp 在 00864B↔MMF tilt、受硬地板 clip，源 **FRED** `fredgraph.csv` 公開端點免 key）。**M2 於 2026-06-23 實測 FRED 抓到真資料後上線**（雙閘＝`enabled_layers` 含 M2 + `M2.enabled:true`）。再平衡觸發＝月初/regime變/usd變（**非每日連續**）。引擎＝`src/strategy_engines/allocator_engine.py`、`main.py` allocator 任務、`src/data/macro_fetcher.py`(M2)、`src/execution/mmf_sleeve.py`(MMF)。
  - **部署＝24/7 launchd**（`com.tradingbot.start` KeepAlive+RunAtLoad + `start_bot.sh` 內 caffeinate；**筆電須開蓋**，闔蓋強制睡）取代原「每日 08:30 開/15:00 關」排程——舊排程因 **Mac 睡眠致 08:30 從未真正觸發、bot 自 ~6/16 起盤中缺席、零交易**（dashboard/LINE 看似壞其實是 bot 沒跑）。dashboard 也 launchd 化（`com.tradingbot.dashboard`）綁 **0.0.0.0:8787**。**停機須 `launchctl bootout`**（KeepAlive 下 kill 會被秒拉起）、更新碼＝`launchctl kickstart -k`。詳見 `docs/M5_DEPLOYMENT_PLAN.md` §14。
  - **🛑 定位（紀律不變、勿誤讀為翻案）**：allocator＝**使用者選定的「多資產分散 + regime 風控」組合，非經證實 alpha**。下方 R5 裁決（誠實池無穩健 alpha、0050 buy-hold 報酬王）**仍成立、未被翻案**。此為鐵則#2「使用者拍板部署」分支下的 **paper 評估**（≥十幾天），**非研究 Gate 通過**。survivorship 上界 caveat 不變。
  - **2026-06-23 三個 live bug 修復**：① benchmark `decide_rebalance` 現金約束用原價估、下單用 fill(含滑價) → 目標 100% 全現金時首筆建倉 `insufficient_cash` 被拒（加 `slippage` 參數 + 回歸測試）；② `tests/conftest.py` per-test 清檔會清掉 **live 帳本**（加 session 級備份/還原；**跑 pytest 前仍建議停 bot**）；③ 滿倉 cash=0 時 `TOTAL_CAPITAL=get_balance() or 50_000` 錯設 50k（改 `_capital_base()` fallback 初始資金）。`pytest` **328 passed**。
  - **下一步**：監看 paper ≥十幾天；real-money（永豐 Shioaji）已記錄計畫但先不實作（paper 先跑）。

- **↓ 以下 R0–R6 + E1–E8 為「導向上述部署的研究歷史」（結論仍為真，但其描述的『當時 live』已被上述 allocator 取代——勿當現況 live 引用）：**
- 本專案的**歷史回測選股池（35 檔手挑 `LIVE_UNIVERSE`）經 Phase 9 證實含重大後見之明污染**：
  該名單是 2026 用「近 3 年 CAGR」事後挑 AI 贏家（`AI_ADOPTED` 更用 2023–25 績效分窗挑出＝直接 look-ahead）、剔落後股。
  量化：**同規模手挑溢價 = +0.38 pooled OOS Sharpe / +9pp 年化（下界，倖存者未除）**。
- 因此**以下數字一律「錯誤被污染、被高估」，不得當定論引用、不得據以調 live**：
  全期 **12.7% / Sharpe 1.16 / DD −16.0% / 總報酬 152%**、Phase 6/7/8 的一切結果、§6.2/6.3 漏斗、`a1_*`/`a2_*`/`exit_sweep`/`sizing_*` 等旁支絕對數字。
- **誠實真相（R0 已執行，2026-06-17；此為新真實 baseline，取代上面污染數字）**：在**無 look-ahead 機械 PIT universe**（`src/backtest/pit_universe.py`：1706 四方完整池、季 reselect、trailing-60d 成交額 top-K + 上市滿 1y + 價格下限）上跑**完整 live edge**（TA+籌碼+block_only regime+vol_target、`adjust=True`、純快取），對照預先指定被動（`notebooks/r0_honest_baseline.py`）：
  - **OOS(2022–25) pooled Sharpe 隨 K 在 0.56–1.21 非單調跳動（中位 0.93）** vs 基準B **0.80** / 0050 **0.95** → **與被動大致打平、無穩健 alpha**（IR vs 基準B 僅 1/3 K 為正；全期 Sharpe 0.62–0.75 一致輸被動；最佳 K=150 的 1.21＝三選一 cherry-pick，鄰格 K=100 反而 0.56＝雜訊跡象）。
  - **唯一較穩的相對優勢＝regime 降 DD**：PIT 臂 DD −22~−29% vs 被動 −32~−34%、最差前進年 −15~−17%（仍含存活池灌水、比帳面小＝附錄 B Tier D）。
  - 誠實全期約 **8–10% / Sharpe 0.62–0.75**（含 2018 冷啟），**取代污染的 12.7% / 1.16 / −16%**——手挑溢價確實又大又真。
  - ⚠️ 仍是**上界**（survivorship：FinMind 無下市）；**主動是否有真 alpha：R1 已裁決＝無穩健 alpha（見下）**。注：R0 的 ~0.9 比 Phase 9 粗版 momentum-only 的 ≈0.50 高，係 R0 跑**完整 edge**（籌碼+adjust+乾淨 top-K），非翻案。
- **R1 細網格 walk-forward 裁決（2026-06-17，pit-rebuild；`notebooks/r1_walkforward.py`，結果見 `PIT_REBUILD_PLAN.md` §2「✅ R1 結果」）**：
  - **Q1：K=150 的 1.21＝in-sample cherry-pick（已證實，不採信）。** 18 點細網格 OOS Sharpe 鋸齒無高原（1 SE δ=0.51，150 僅 ~1σ 突出於 ~0.7 雜訊 floor＝孤峰）；walk-forward 每 fold 內選 K **從不選到 150**（落 110/300/400），pooled OOS 崩到 0.07–0.64、**全輸基準B 0.80、IR 全負、選擇不穩**。**ETF-排除 sanity 再坐實**：移除 0050 等 6 檔 ETF 後 1.21@150 崩到 0.89、峰移 K=50＝K 隨池組成重排（150 尖峰部分係策略在持基準 0050/0056）。**→ K 去留已裁決：誠實池無穩健特殊 K，不固定。**
  - **Q2：加格（max_pos>6）僅方向性線索、非穩健翻盤。** in-sample 加格＋budget sizing 明確改善（Sharpe↑/DD↓/集中度 top3 63%→24%，誠實池確比手挑池更需分散＝附錄 B 方向成立）；但 walk-forward N* 在 B1 代表 K 不穩、**IR vs 基準B 全負**（即使 K=150 穩定臂 pooled 1.12 也只是低-DD/低報酬**防禦 profile、非 alpha**）。**→ 不據以調 live；列為 R5 sizing 結構輸入。**
  - **R1 總 Gate：FAIL（無穩健 alpha）。唯一站得住的真 edge＝regime 降 DD**（外層 vs 被動 OOS 成立：wf 最差前進年 −12~−15% vs 被動 −32~−34%）。所有 OOS 仍是**上界**（survivorship）。**→ 下一步＝R-attrib（量化 regime-DD 層＋sizing 線索）＋ R5 誠實出口（被動為主、縮小主動）；total Gate 未過 → live 全不動。**
- **R-attrib 逐層歸因（2026-06-17，pit-rebuild；`notebooks/r_attribution.py`，詳見 `PIT_REBUILD_PLAN.md` §3）**：元件級 ablation（L0 等權PIT→+引擎→+TA→+籌碼gate→+籌碼select→+regime(=R1full)→+動量；etf_excl K=100；Sanity：L5 OOS 0.70 逐字重現 R1）。**結論：無任何層加 robust OOS alpha（signal-layer ΔSharpe 皆在 δ≈0.5 雜訊內、IR vs B 全程<0）；唯 regime 層 K-穩健（跨 K 一致 +17~24pp DD 降）＝防禦非 alpha。籌碼層 standalone 跨 K 變號(−0.53/+0.86/−0.17)、K=100 轉負(PnL 分散 top3 625%)＝master Phase 10#6 疑慮坐實**（唯在 regime 上有條件性 ~6pp DD 助益、未驗 K-穩健）；TA/動量＝雜訊。**→ R5 輸入：active 價值＝regime DD-overlay；極簡 regime+size 地板(0.57/−21%)已捕大半，全 edge 僅多雜訊內 Sharpe+~6pp DD。total Gate 未過 → live 不動。**
- **R5 正式裁決（2026-06-17，pit-rebuild；`notebooks/r5_alpha_verdict.py`，詳見 `PIT_REBUILD_PLAN.md` §5）＝重建收束**：風險對齊 DD 檢定（防禦 sleeve／+chip／全 edge L5 vs 0050、基準B 摻現金 de-risk 到同 vol；Sharpe/Calmar/DD-vol 皆 scale-invariant）＋顯著性（IR bootstrap、α/β Newey-West）。**① 無顯著 alpha**（IR vs B −0.28~−0.38 全負、95% CI 全含 0、α t≈0）；**② regime 降-DD 是「真但不顯著」的防禦、非純恆等式**（同 vol 下 maxDD 比基準B 淺 +3.6~+6.9pp、DD/vol 更優、2022 熊市 sleeve 段 −14.5% vs B@vol −29.5%＝確在擇時砍崩盤；**此修正「純恆等式」的過嚴預期**）；**③ 但對原始 0050 全輸**（0050 OOS Sharpe 0.94/Calmar 0.59、年化 20% vs 主動 8–9%；2023–25 大漲 buy-hold 完勝）、防禦不顯著、報酬代價大 → **不構成 mandate**；④ chip 提升 Sharpe/Calmar 但惡化 DD/vol、不值保留。**→ 總 Gate FAIL → live 全不動。誠實出口＝以被動（0050）為主；regime 防禦 sleeve 僅在明確 drawdown mandate 下小規模可選。** survivorship → 全為上界、結論更穩。**現行 live 手挑 35 檔帳面 edge 經證為後見之明、前瞻無 alpha；重建 R0→R5 誠實收束，R2/R3/R4 已被涵蓋跳過、R6 僅在使用者選落地時執行。**
- **R6 被動落地 + 倉庫清理（2026-06-17；使用者拍板，已執行）**：live 改**被動為主**＝**0050 vol-target + MA200 overlay**。最終參數（細網格退場深度 0~100% 掃描後定）：`config/settings.yaml` `strategy.mode: benchmark`、`benchmark`＝0050 / **target_daily_vol 1.0（停 vol-cap＝平時 100% 跟 0050）/ regime_overlay true / regime_ma 200 / regime_action 0.85（跌破 MA200 留倉 85%、漲回回滿）**；引擎 `regime_action` additive 支援數值（zero/half 不變）。**舊 active 籌碼策略「直接執行路徑」已刪除**（`score_engine`/`chip_signal`/`active_engine` + `main.py` active 任務/排程；`make_engine` 改 benchmark-only fail-safe）；**研究回測路徑保留**（`tech_signal`/`capitulation`/`capped_sim`/`signal_builder` + `strategy.yaml` 為 research 配置——反向相依 grep 驗證不可刪）。`pytest` **98 passed**。研究歷程統整於 **`docs/RESEARCH_JOURNEY.md`**。**定位＝結構性降回撤規則、非經證實 outperformer（R5 無顯著 alpha）。** 待使用者部署（重啟 main.py＋清 `data/processed/paper_account.json` 孤兒倉）；rollback＝`strategy.mode` 回 active（但 active 執行碼已刪，僅 fail-safe 回 benchmark）。**⇒ live 已從手挑 35 檔 active 轉為被動 0050+MA200-85% overlay。**
- **E1+E2 whipsaw 修正整併入 live（2026-06-18；使用者拍板，已執行）**：承事件研究（`docs/EVENT_DETECTION_RESEARCH.md`、`docs/DRAWDOWN_EVENT_STUDY_2020_2022.md`）發現 MA200 **每日**規則有「初跌無保護（滯後）＋ whipsaw（2022 在 3.5 週 7 次穿越）」盲區 → 沙盒試做 E1（N 日連續確認）/E2（對稱緩衝帶）/E3（ATR 帶），經正式 walk-forward OOS 驗證（`docs/E1_E3_COMPARISON.md`、`docs/E1_E2_WALKFORWARD.md`；`notebooks/e1*`、`e1e2_walkforward.py`、`e1e2_combined_validate.py`；純快取）。**裁決：結構 Gate PASS（跨 4 fold OOS：降 whipsaw + DD 不惡化且優於基準B/0050 + 牛市不犧牲 + 2020 不 over-lag）；alpha Gate 仍 FAIL。** ⚠️ **方法論修正（重要、勿再誤引）**：walk-forward 的「IRvs基準B ≈ +1.1」是 **beta 不是 alpha**（基準B 為 de-risked 低曝險）——鐵證＝**0050 買持自身 IRvs基準B = +1.00**（純 beta、零技巧）；**真 alpha 檢定＝同 beta 的 IRvs0050 = −0.18/−0.27（負/零）、OOS Sharpe 邊際 ≪ δ=0.51 ＝無顯著超額**（R5「無 alpha、0050 報酬王」未翻案）。落地內容＝把 R6 既有防禦 overlay 的「跌破態」判定由**每日 MA200** 改為**連 3 日跌破 MA200×0.99 才砍至 85%、連 3 日站回 ×1.01 才回滿**（combined N=3 + band 1%，**穩健高原值非 in-sample 峰**：N=3＝Alvarez 文獻、1%≈0050 一日移動）：`config/settings.yaml` 新增 `regime_confirm_days: 3`、`regime_band_pct: 0.01`；引擎 `benchmark_engine.py` 新增 `_regime_below` 狀態機 + `vol_target_exposure(regime_confirm_days=1, regime_band_pct=0.0)` ＝ **additive 行為中性（預設＝舊每日規則，98 原測逐位不變）**。效果（2018-25 純快取、survivorship 上界）：**2022 假穿越 7→1、最差前進年 DD −31.2→−30.5%、交易 126→105、2018/2022 報酬+Sharpe 皆優於舊規則、2020 V 急崩無 over-lag**。3 個獨立 agent 驗證全過（行為中性 max|Δ|=0、新 config ≡ 沙盒數字 17/17、對抗 review 無 look-ahead/邊界/wiring 問題）；`pytest` **105 passed**（舊 98 + 新 7）。**定位＝現行防禦 overlay 的結構性降 whipsaw 微調、非 alpha**（不違反鐵則#2 的精神：R6 overlay 本就以**防禦**而非 alpha 落地，此為其 whipsaw 行為的 OOS-驗證微調；alpha 仍 FAIL 已誠實記錄）。待使用者重啟 main.py 生效；rollback＝`config` 兩鍵回 `1`/`0.0`（即復舊每日 MA200 規則）。**⇒ live 現＝0050 + MA200（連 3 日確認 + 1% 緩衝帶）-85% overlay。**（⚠️ 此為 2026-06-18 當時狀態；**2026-06-23 已切 6 資產 allocator**，見頂部 🟢 LIVE 現況）
- **事件偵測延伸研究全數 FAILED、live 自 E1+E2 起未再動（2026-06-18；純沙盒、未碰 live/engine/config、已歸檔）**：沿 `docs/EVENT_DETECTION_RESEARCH.md` roadmap 把「比 MA200 更早/更準的崩盤偵測」逐一試到底——**E4（第二道防線 vol-spike/from-peak）/E5（組合閘+外資）/E7（美股半導體當砍倉時點）/E7b（美股確認延續→砍倉深度）全 FAILED**；**E8（新聞情緒 ex-post）僅規劃未執行**（`docs/E8_PLAN.md`：2020 急崩在 FinMind 新聞窗 2020-04 之前、唯 2022 OOS 崩盤 n=1→結構上不可 walk-forward、永不碰 live）。每條皆多 agent 沙盒（plan→build→對抗驗證→synthesis）、純快取、walk-forward OOS、**beta/alpha 分離**、survivorship 上界。**關鍵發現＝美股半導體（^SOX/SMH/TSM ADR，FinMind `USStockPrice`）對 0050 領先是真的（corr 0.55、Granger 單向 p≈1e-115、跨期穩定），但 ~99% 在 0050 開盤跳空被吸收、盤中≈隨機 → 不可萃取**（`notebooks/us_lead_0050.py`）；E7（時點）賣在跳空+86% 假警報反傷、E7b（深度）matched-D 對照證 US-conditioning 不如 flat-deep（無條件砍更深）。**無一翻案 R5「無 alpha、0050 報酬王、regime 僅防禦」。** 報告：`docs/E4_E5_COMPARISON.md`、`docs/E7_US_SEMI_DEFENSE.md`、`docs/E7B_DEPTH_MODULATION.md`（皆標 🟥 FAILED banner）。**唯一負結果線索**：若有明確 drawdown mandate，降 DD 槓桿＝flat-deep（`regime_action` 砍更深 0.70/0.60）＝沿 R6 已知前緣的**風險偏好抉擇、非 alpha**（R6 已選 0.85）。**⇒ live 仍＝0050 + MA200（連3日+1%帶）-85% overlay，自 E1+E2 起未動；E4–E8 全為失敗/未執行研究歸檔。**（⚠️ **2026-06-23 起 live 已切 6 資產 allocator**，見頂部 🟢 LIVE 現況）

## 📌 權威文件（真相地位，依序必讀）

0. **`docs/RESEARCH_JOURNEY.md`** ← **完整研究歷程單一回顧**（Phase 0→11 + PIT 重建 R0→R6 + 倉庫清理紀錄 + 檔案清單）。要「整段故事」看這份。
1. **`docs/PIT_REBUILD_PLAN.md`** ← 乾淨 PIT 重建逐步 playbook（**R0→R6 全完成：總 Gate FAIL＝被動為主、R6 落地 0050+MA200-85% + 舊 active 執行碼刪除**；R2/R3/R4 已被涵蓋跳過；結果見 §2/§3/§5/§6）。
2. **`docs/IMPROVEMENT_PLAN_v2.md` 附錄 B** ← 全專案 universe 後見之明污染稽核（Tier A–D 受污染清單、含被否決結論）。
3. `taiwan_trading_bot_master.md` 第六章 ＋ `docs/IMPROVEMENT_PLAN_v2.md` 本文 ← 歷史脈絡，但其數字已就地標 🟥「錯誤被污染」，**僅供脈絡、非定論**。

## ✅ 鐵則（所有 session 遵守）

1. **不得**把上述污染數字當有效結論引用，**不得**據舊污染結論改 live（`config/strategy.yaml`、`src/`）。
2. 在乾淨 PIT 重建（`PIT_REBUILD_PLAN.md` R0–R5）通過**總 Gate（OOS 勝基準B，或 DD 優勢單獨成立）前，live 全不動**。
3. 每個實驗綁 **walk-forward OOS**，in-sample 只當線索；對照基準**固定預先指定**：0050 買持 + 基準B（vol_target 0.011，**非** best-of-sweep）。
4. 回測**純快取、不打 API**（背景 full-market builder 在跑、共用 FinMind 配額）；引擎改動須**行為中性 additive**（新參數預設＝舊行為，中性檢查把關）。
5. **倖存者偏誤無法消除**（FinMind 此 stack 無下市/歷史成分）→ 即使「誠實 PIT」結果仍是上界，結論須帶此 caveat。
6. **不自動 push；commit 僅在使用者明說時**；commit 排除 runtime 產物（`data/archive/*`、`logs/*`、`start.sh.save`）。
7. **參數掃描一律用細網格**：凡「調某一參數看結果」的掃描，單一參數至少 ~12–18 點、核心區間步長要小，**不得用 3–4 點粗掃下結論** —— 粗掃無法分辨「平滑高原＝訊號」vs「鋸齒跳動＝雜訊」（R0 的 K=50/100/150 → OOS 0.93/0.56/1.21 即無法解讀的教訓）。細掃是**特徵化/找線索**，決策仍只綁 walk-forward OOS，**不得用 in-sample 峰值挑參數**（那＝把 OOS 變 in-sample 的 cherry-pick）。
8. **舊 Phase 6/7/8 的絕對門檻不可移轉到誠實池**：那些 DD floor（−18% 軟／−20% 硬 REJECT）、Calmar/Sharpe 絕對目標係按**污染手挑池**（DD −14..−16%）校準；誠實 PIT 池 DD 結構性更深（R0：−22..−29%，**但仍優於被動 −32..−34%**），直接套絕對 floor 會 REJECT 全部格、選擇退化成永遠 fallback。重建期所有 reused gate 一律**重錨**：DD／績效門檻錨到 ①預先指定被動基準（基準B／0050），②同族相對比較（如 max_pos 錨 N=6）。**只有相對／結構準則可移轉，絕對數字一律退役。**

## 🧭 專案定位（快速 orientation）

- 台股 paper-trading bot；**live＝6 資產 allocator**（`config/settings.yaml strategy.mode=allocator`＝0050/00981A/00991A/00635U/00864B/MMF，M0+M1+M2 三層全開；詳見頂部 🟢 LIVE 現況；引擎 `src/strategy_engines/allocator_engine.py` + `main.py` allocator 任務 + `macro_fetcher.py`(M2) + `mmf_sleeve.py`(MMF)）。**前一階段 benchmark**（`mode=benchmark`＝0050 vol-target + MA200-85% overlay + E1+E2 連3日/1%帶；引擎 `benchmark_engine.py` 保留、rollback 可回）。`config/strategy.yaml` 現為**研究回測**配置（舊 active 參數；capped_sim/signal_builder/tech_signal 讀取），不再被 live 執行。研究回測引擎＝`src/backtest/capped_sim.py`（`run_capped`）。
- 既有 walk-forward / grid harness 可複用：`notebooks/p8_walkforward.py`、`p6_*`、`p7_exit_diag.py`、`p9_*`、`r0_*`/`r1_*`/`r_attribution`/`r5_*`/`r6_*`、`benchmark_backtest.py`。
- **PIT 重建進度**：廣池籌碼 builder **已完成**（四方完整 **1706**）；**R0 + R1 + R-attrib + R5 已執行完畢（重建收束）**（R0：`r0_*`+`pit_universe.py`；R1：`r1_walkforward.py`+`r1_base_sig.pkl`；R-attrib：`r_attribution.py`+`r_attrib_base.pkl`；R5：`r5_alpha_verdict.py`，結果見「現況真相」對應區 ＋ `PIT_REBUILD_PLAN.md` §2/§3/§5）。裁決：K=cherry-pick、加格僅方向性、無任一層加 robust alpha、**唯 regime 崩盤防禦真但不顯著且不敵 0050 buy-hold → 無可前瞻複製 alpha、被動為主誠實出口、總 Gate FAIL**。**R6 已執行＝被動落地 0050+MA200-85% overlay + 倉庫清理**（細節見「現況真相」R6 條 ＋ `RESEARCH_JOURNEY.md`）：benchmark mode（target_vol 1.0、regime_action 0.85）、**舊 active 執行碼已刪**、研究保留、`pytest` **98 綠**。**重建 R0→R6 完成。** 待使用者部署（重啟 main.py＋清 paper 孤兒倉）；後續：部署後監看；或另開因子研究（超出範圍）。R2/R3/R4 已被涵蓋跳過。
- **`pit-rebuild` 已整併為單一乾淨主線**（benchmark 85% live + 研究保留 + 舊 active 執行碼刪除 + `RESEARCH_JOURNEY.md`）；可作新 main（使用者決定 rename/merge/push）。`r6-passive-landing` 為中途 deployable 快照（已被 pit-rebuild 涵蓋）。

---

*建立 2026-06-16｜更新 2026-06-23（**M5 6 資產 allocator 上線 paper**：benchmark→allocator、M0+M1+M2 三層全開（M2 接 FRED CPI+Fed）、24/7 launchd 部署取代失效的每日排程、3 個 live bug 修復（slippage sizing／conftest 清帳本／TOTAL_CAPITAL 50k）、`pytest` 328 綠；見頂部 🟢 LIVE 現況 ＋ `docs/M5_DEPLOYMENT_PLAN.md`）｜前次 2026-06-18b（**E4–E8 事件偵測延伸研究全 FAILED/僅規劃、歸檔、live 自 E1+E2 起未再動**：E4/E5/E7/E7b FAILED + E8 僅規劃，美股半導體領先 0050 為真但 ~99% 在開盤跳空被吸收不可萃取，無一翻案 R5；統整於 `docs/RESEARCH_JOURNEY.md` §(g)；研究歸檔 commit 與 live 變更 commit[6bf494c]分離）｜前次 2026-06-18a＝**E1+E2 whipsaw 修正整併入 live**（MA200 每日→連 3 日確認 + 1% 緩衝帶[`regime_confirm_days=3`/`regime_band_pct=0.01`]，結構 Gate PASS / alpha FAIL、additive 行為中性、`pytest` 105 綠，§(f)）｜2026-06-17＝R6 被動落地 0050+MA200-85% + 舊 active 執行碼刪除｜性質：standing 真相與紀律。*
